import styled from "styled-components";

const AppStyle = styled.div`
  max-width: 1400px;
  margin: auto;

  .change-pct-24h {
    width: 80px;
    margin-left: 15px;
  }
`;

export default AppStyle;
